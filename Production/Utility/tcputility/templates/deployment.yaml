apiVersion: v1
kind: Service
metadata:
  name: tcputility-service
spec:
  selector:
    app: tcputility-app
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 8080
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tcputility-app
spec:
  selector:
    matchLabels:
      app: tcputility-app
  replicas: 1
  template:
    metadata:
      labels:
        app: tcputility-app
    spec:
#      affinity:
#        nodeAffinity:
#          requiredDuringSchedulingIgnoredDuringExecution:
#            nodeSelectorTerms:
#              - matchExpressions:
#                  - key: kubernetes.io/hostname
#                    operator: In
#                    values:
#                      - edgenode-pi
#                      - edgenode-ubuntu
      containers:
        - name: tcputility-app
          image: savantripathi22/tcputility:aug23
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      nodeSelector:
          kubernetes.io/hostname: edgenode-pi
      tolerations:
        - key: "node-role.kubernetes.io/edge"
          operator: "Exists"
          effect: "NoSchedule"
