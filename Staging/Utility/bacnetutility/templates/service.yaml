apiVersion: v1
kind: Service
metadata:
  name: {{ include "bacnetutility.fullname" . }}
  labels:
    {{- include "bacnetutility.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bacnetutility.selectorLabels" . | nindent 4 }}
