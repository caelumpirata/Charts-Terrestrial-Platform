apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bacnetutility.fullname" . }}-test-connection"
  labels:
    {{- include "bacnetutility.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bacnetutility.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
