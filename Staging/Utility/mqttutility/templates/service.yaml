apiVersion: v1
kind: Service
metadata:
  name: {{ include "mqttutility.fullname" . }}
  labels:
    {{- include "mqttutility.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mqttutility.selectorLabels" . | nindent 4 }}
