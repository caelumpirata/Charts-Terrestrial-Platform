apiVersion: v1
kind: Service
metadata:
  name: {{ include "opcuautility.fullname" . }}
  labels:
    {{- include "opcuautility.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opcuautility.selectorLabels" . | nindent 4 }}
