apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opcuautility.fullname" . }}-test-connection"
  labels:
    {{- include "opcuautility.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opcuautility.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
