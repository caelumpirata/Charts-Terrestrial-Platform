apiVersion: v1
kind: Service
metadata:
  name: {{ include "tcputility.fullname" . }}
  labels:
    {{- include "tcputility.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tcputility.selectorLabels" . | nindent 4 }}
