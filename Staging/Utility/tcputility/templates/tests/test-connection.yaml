apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tcputility.fullname" . }}-test-connection"
  labels:
    {{- include "tcputility.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tcputility.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
